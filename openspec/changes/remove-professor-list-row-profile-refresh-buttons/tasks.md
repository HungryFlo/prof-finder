## 1. Implementation

- [x] 1.1 在 `ProfessorListView.vue` 的操作列中移除「更新」「画像」两个 `NButton`（保留「详情」与「删除」等其余行内操作）。
- [x] 1.2 若 `handleRefresh` / `handleGenerateProfile` 仅服务于上述行内按钮，则删除这两个函数及相关仅用于它们的导入或逻辑，避免死代码。
- [x] 1.3 视需要收窄操作列 `width`（原先为容纳多按钮而设宽）。
- [x] 1.4 手动回归：访问 `/professor`，确认无行内「更新」「画像」；勾选多行后「批量更新」「批量生成画像」仍可用；单条刷新/画像在详情页仍可用（若产品已有该页能力）。
